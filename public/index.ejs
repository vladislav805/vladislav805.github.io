<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
        <meta name="description" content="Web-developer from Saint-Petersburg." />
        <title><%= title %></title>
        <% metaOG.forEach(function(item) { %>
            <meta property="<%= item.property %>" content="<%= item.content %>" />
        <% }) %>
        <% files.css.forEach(function (src) { %>
            <link rel="stylesheet" href="<%= src %>">
        <% }) %>
    </head>
    <body>
        <div id="root"></div>
        <% files.js.forEach(function(src) { %>
            <script src="<%= src %>" defer></script>
        <% }) %>
        <!-- Yandex.Metrika counter -->
        <!--suppress CommaExpressionJS -->
        <script type="text/javascript">(function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)}; m[i].l=1*new Date();k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)}) (window, document, "script", "https://cdn.jsdelivr.net/npm/yandex-metrica-watch/tag.js", "ym"); ym(16828789, "init", { clickmap:true, trackLinks:true, accurateTrackBounce:true });</script>
        <!-- /Yandex.Metrika counter -->
    </body>
</html>
